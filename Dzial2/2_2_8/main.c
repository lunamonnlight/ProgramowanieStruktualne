#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
    double n, pierw;
    int wynik;
    printf("Podaj n: \n");
    scanf("%lf", &n);
    pierw = sqrt(n);
    int i = n-3;
    for (i;i<=n;i++)
    {
        wynik = i;
    }
    printf("%d", wynik);
    return 0;
}
